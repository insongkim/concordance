% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hs1_to_sitc4.R
\name{concord_hs1_sitc4}
\alias{concord_hs1_sitc4}
\title{Converting HS1 to SITC4 Codes}
\usage{
concord_hs1_sitc4(sourcevar, origin, destination, dest.digit = 5,
  all = FALSE)
}
\arguments{
\item{sourcevar}{An input character vector of HS1 codes. Allows 6, 4, or 2 digits.}

\item{origin}{A string setting the input industry classification (HS1).}

\item{destination}{A string setting the output industry classification (SITC4).}

\item{dest.digit}{An integer indicating the preferred number of digits for output codes. Allows 1 to 5 digits. The default is 5 digits.}

\item{all}{Either TRUE or FALSE. If TRUE, the function will return (1) all matched outputs for each input, and (2) the share of occurrences for each matched output among all matched outputs. Users can use the shares as weights for more precise concordances. If FALSE, the function will only return the matched output with the largest share of occurrences (the mode match). If the mode consists of multiple matches, the function will return the first matched output.}
}
\value{
The matched output(s) for each element of the input vector. Either a list object when all = TRUE or a character vector when all = FALSE.
}
\description{
\code{concord_hs1_sitc4} converts HS1 to SITC Revision 4 codes.
}
\examples{
# one input: one-to-one match
concord_hs1_sitc4(sourcevar = "120600", origin = "HS1", destination = "SITC4", all = FALSE)
concord_hs1_sitc4(sourcevar = "120600", origin = "HS1", destination = "SITC4", all = TRUE)

# two inputs: multiple-to-one match
concord_hs1_sitc4(sourcevar = c("010111", "010119"), origin = "HS1", destination = "SITC4", all = FALSE)
concord_hs1_sitc4(sourcevar = c("010111", "010119"), origin = "HS1", destination = "SITC4", all = TRUE)

# two inputs: repeated
concord_hs1_sitc4(sourcevar = c("120600", "120600"), origin = "HS1", destination = "SITC4", all = FALSE)

# sourcevar has different number of digits, gives an error
concord_hs1_sitc4(sourcevar = c("120600", "1206"), origin = "HS1", destination = "SITC4", all = FALSE)

# sourcevar includes NAs, gives an error
concord_hs1_sitc4(sourcevar = c("120600", NA), origin = "HS1", destination = "SITC4", all = FALSE)

# if no match, will return NA
concord_hs1_sitc4(sourcevar = c("120600", "120800"), origin = "HS1", destination = "SITC4", all = FALSE)

# 5-digit inputs not supported and will give an error
concord_hs1_sitc4(sourcevar = c("12060", "85460"), origin = "HS1", destination = "SITC4", all = TRUE)

# 4-digit inputs
concord_hs1_sitc4(sourcevar = c("1206", "8546"), origin = "HS1", destination = "SITC4", all = TRUE)

# 4-digit outputs
concord_hs1_sitc4(sourcevar = c("120600", "010111"), origin = "HS1", destination = "SITC4", dest.digit = 4, all = TRUE)
}
